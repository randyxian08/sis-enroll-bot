# SIS 选课机器人使用教程

> 从零开始，15 分钟跑通。不需要编程基础，照着做就行。

## 目录

1. [原理一句话](#原理一句话)
2. [准备工作](#准备工作)
3. [第一步：安装 Kimi WebBridge](#第一步安装-kimi-webbridge)
4. [第二步：登录 SIS 并准备好选课车](#第二步登录-sis-并准备好选课车)
5. [第三步：注入机器人](#第三步注入机器人)
6. [认识控制面板](#认识控制面板)
7. [第四步：演练（必做）](#第四步演练必做)
8. [第五步：正式开抢](#第五步正式开抢)
9. [捡漏模式：蹲别人 drop 出来的空位](#捡漏模式蹲别人-drop-出来的空位)
10. [常见问题](#常见问题)

## 原理一句话

机器人不住在服务器上，它就住在**你自己浏览器里那个已经登录的 SIS 页面里**。
它做的事情 = 你手动做的事情（点 Enroll → 点 Finish Enrolling），只是它在开闸那一瞬间以毫秒级速度替你点完，还会提前对好表、提前把网络连接捂热。

所以：

- 不需要把账号密码交给任何人；
- 不绕开学校任何限制——封窗期间服务器照样拒绝，机器人只是「到点第一个敲门的人」；
- 关掉浏览器 = 机器人下班。

## 准备工作

| 需要的东西 | 说明 |
| --- | --- |
| 一台电脑（macOS / Windows 均可） | 开抢当天保持亮屏、插电源、网络稳定 |
| Chrome 或 Edge 浏览器 | 能装 Chrome 扩展即可 |
| Kimi WebBridge 扩展 | 让脚本和你浏览器里的 SIS 页面通信的桥梁 |
| 你自己的 HKU 账号 | 机器人用的是你已登录的会话 |

## 第一步：安装 Kimi WebBridge

1. 安装 Kimi Code CLI（命令行工具），终端执行：

   ```bash
   curl -fsSL https://code.kimi.com/install.sh | bash
   ```

2. 安装 WebBridge 浏览器扩展：按 Kimi Code 文档指引，从 Chrome 应用商店安装，或在 `chrome://extensions` 开启「开发者模式」后加载扩展目录。
3. 启动本地桥接服务，终端执行：

   ```bash
   kimi webbridge start
   ```

4. 验证桥接正常：

   ```bash
   curl -s http://127.0.0.1:10086/status
   ```

   看到 `"running":true` 即成功。

## 第二步：登录 SIS 并准备好选课车

1. 用 Chrome 正常登录 <https://sis-main.hku.hk>。
2. 进入 **Enrollment → Shopping Cart**，把要选的课**全部提前加进选课车**（Temporary Course List）。
3. 注意：**Sem 1 和 Sem 2 是两个独立的车**，两边的课都要提前加好。
4. 提前自己走一遍流程确认：每门课没有前置要求问题、没有时间冲突。机器人不会替你解决这些——服务器拒了就是拒了。

## 第三步：注入机器人

1. 下载本仓库，终端进入目录：

   ```bash
   git clone https://github.com/randyxian08/sis-enroll-bot.git
   cd sis-enroll-bot
   ```

2. 让脚本帮你打开选课车页面（或者手动切到那个标签页）：

   ```bash
   ./sis-bot.sh nav cart
   ```

3. 注入：

   ```bash
   ./sis-bot.sh inject
   ```

4. SIS 页面右下角会出现一个深色悬浮卡片，写着「SIS 选课机器人」——这就是控制面板，说明机器人已上线。

## 认识控制面板

面板有三个页签，所有功能都能用鼠标完成：

- **抢课**：设置 Sem 1 / Sem 2 的开闸时间，三个按钮——演练、开始抢课、停止。
- **捡漏**：填写目标课号，查状态、开始蹲守、停止。
- **日志**：两个引擎的实时日志合并显示，红色是出错、绿色是成功、黄色是发现空位。

面板可以用鼠标拖着移动，点右上角的「—」折叠。你填的配置会自动保存，刷新页面不丢。

> 喜欢命令行的话，`sis-bot.sh` 的每个子命令和面板按钮一一对应，效果完全相同。

## 第四步：演练（必做）

点面板上的「演练」，或者终端执行：

```bash
./sis-bot.sh dryrun
```

机器人会完整走一遍「点 Enroll」的流程，但**在真正提交前停手**，并报告：

- `blocked: true` → 正常！说明流程通了，只是现在还没到开放时间；
- 报错 → 按提示排查（课程没加进车、页面不对等）。

> 演练不会产生任何真实选课记录。

## 第五步：正式开抢

在面板上设好两个开闸时间，点「开始抢课」。或者终端执行：

```bash
./sis-bot.sh start "2026-08-18T10:00:00+08:00" "2026-08-18T10:10:00+08:00"
```

- 时间按学校当年公告填，上面是 2026-27 学年的例子：Sem 1 于 10:00 开闸，Sem 2 于 10:10 开闸。
- 机器人会自动对表（误差 ±60ms）、预热连接、到点毫秒级提交，Sem 1 成功后自动切 Sem 2。
- 保持这个标签页在前台，电脑别休眠。日志页签能看到每一步。
- 结束后到 SIS 的 **Enrollment Status** 页面核对：全打勾 = 成功；有叉 = 前置/冲突问题，去手动处理。

## 捡漏模式：蹲别人 drop 出来的空位

课满了没抢到？add/drop 期间每天都有人退课，空位是随机出现的。捡漏模式帮你 24 小时盯着，空位一出现立刻替你提交——比任何人手动刷新都快。

1. **查课号**。捡漏用的是 Class Nbr（4-5 位数字），不是课程代码。打开 SIS 的 Class Search 搜到那门课，结果里的「Class Nbr」列就是；已经上过/选过的课在课表里也能看到。
2. 打开 Add Classes 页面并注入：

   ```bash
   ./sis-bot.sh nav add
   ./sis-bot.sh inject-snipe
   ```

3. 在面板「捡漏」页签填目标，每行一个：`课号,学期`。例如：

   ```
   12345,1
   23456,2
   ```

   表示蹲 Sem 1 的 12345 和 Sem 2 的 23456。
4. 先点「查一次状态」验证课号没填错——会返回每门课当前是 Open 还是 Closed。
5. 点「开始蹲守」。机器人每隔几秒（默认 5 秒，建议不要低于 3 秒）自动检查一次，发现 Open 立即走完整报名流程。

命令行等价操作：

```bash
./sis-bot.sh check "12345,1 23456,2"     # 查一次状态
./sis-bot.sh snipe "12345,1 23456,2" 5000 # 开始蹲守，间隔 5000ms
./sis-bot.sh snipe-status                # 看日志
./sis-bot.sh snipe-stop                  # 停止
```

注意事项：

- 蹲守是长跑，电脑合盖、断网、浏览器关闭都会让它下班。想过夜蹲守，确认电源和网络。
- 轮询间隔有 ±20% 随机抖动，请求特征不像机械流量。
- 目标课显示 Wait List 时机器人只记录不提交（排队规则另算，不建议自动排）。

## 常见问题

**Q：机器人会泄露我的账号吗？**
A：不会。所有代码都在你本地跑，用的就是你浏览器里已登录的会话，不外传任何凭据。

**Q：会不会被学校封号？**
A：机器人只在学校允许的开放时间内发正常请求，内容和手动操作完全一致。捡漏模式请把间隔保持在 3 秒以上。抢课行为本身请遵守学校规定，后果自负。

**Q：Mac 合盖行不行？**
A：不行。合盖 = 浏览器睡眠 = 机器人下班。系统设置里关掉自动睡眠，插着电源。

**Q：页面提示会话超时？**
A：SIS 会话约 20 分钟无操作会掉。机器人运行期间会自动保持活跃，但注入前请确认你刚登录不久。

**Q：注入后没看到面板？**
A：确认当前标签页就是 SIS 页面（选课车或 Add Classes），再重新 inject 一次。还不行就刷新页面后重新登录再注入。

**Q：支持给别人用吗？**
A：每个人用自己的浏览器、自己的账号、自己的电脑各自部署一份即可。
